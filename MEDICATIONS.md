# 用药与补剂

一张回答**「我和药/补剂是什么关系」**的表:他在吃什么、需要时吃什么、试过什么没用、什么
不能吃。模型每轮都看得见它,用户能在一个独立入口里改它,也能拿某一种药当话题单独聊。

本文档是设计与任务拆分。落地之后架构决策那几段要并进 `CLAUDE.md`。

## 一、它不是一个用药打卡本

Apple Health 从 iOS 26 起有一套完整的用药追踪(排程、提醒、打卡、依从性),HealthKit 也把它
开放给第三方**只读**(`HKUserAnnotatedMedicationQuery` 拿清单、`HKMedicationDoseEvent` 拿
服药记录,两者的 `init` 都是 `NS_UNAVAILABLE`)。

**那套回答的是「我今天该吃什么、吃了没」。这张表回答的是「我和这个东西是什么关系」。**

后者是前者永远拿不到的——`HKMedicationDoseEvent` 里没有一个字段叫「吃了没感觉」。而这个
功能的产生场景本来就是一次问答的结果:用户问「我头疼吃什么」「冬天要不要补点什么」,Vana
答了,他决定试试——**要记下来的是这次判断和后来的结果**,不是一条排程。

所以这张表最值钱的一列是 `outcome`(对我有没有用),不是剂量。剂量和依从性是 Apple Health
的事,Vana 不抢(见第八节)。

## 二、定位:形状最接近记忆

|              | 健康数据      | 用药与补剂           | 记忆            |
| ------------ | ------------- | -------------------- | --------------- |
| 从哪来       | 传感器,查出来 | 一次问答的结果       | 用户说的 / 抽的 |
| 会不会过期   | 每天都变      | 几个月不变           | 不变            |
| 模型怎么拿到 | 调工具        | **常驻 system 段**   | 常驻 system 段  |
| 用户能不能改 | 不能          | **能,这是重点**      | 能              |

边界抄记忆那一套,不要照着 `HealthTools` 抄——那套是给「每次都要重查」的东西设计的。

**那为什么不干脆做成 `MemoryKind.medication`?** 四条:

- 记忆是**一句话**,这里一条要有结构(名字/状态/什么情况下吃/效果)。塞进一句话就做不了
  「独立入口 + 可编辑 + 按状态分组」。
- 记忆有 40 条 / 2000 字的**共享上限**。吃十种补剂的人会把「他上夜班」挤掉。
- 记忆的淘汰是「挤掉最久没更新的」。对「我对青霉素过敏」这条,那是致命的。
- 用户要一个独立入口去看和改——这本身就说明它是一等对象,不是记忆的一个子类。

**共享的规矩**:`origin` 三态、pinned 不被抽取器动、快照绑会话、末尾一句防线、followUp 回访。

## 三、四种状态,按要紧程度排

| 状态 | 例子 | 排序理由 |
| --- | --- | --- |
| **不能吃** | 青霉素过敏、乳糖不耐 | 安全相关。永远排第一,永不淘汰,永不因为容量被裁掉 |
| **长期在吃** | 他汀、降压药、每天的维生素 D | 是所有健康分析的前提(见第四节) |
| **需要时吃** | 头疼吃布洛芬、睡不着吃褪黑素 | 带触发条件,回答「我现在这情况该吃点什么」时要看 |
| **试过了** | 褪黑素试了两周没感觉 | 防止 Vana 再推荐一次他已经试过的东西 |

**第四类是这个功能的隐藏价值。** 用户问「睡不好怎么办」,Vana 第二次又建议褪黑素——而他
三个月前试过没用、还专门记了下来。那一刻这个 app 就显得没在听。这一类不需要多准,只需要
在场。

## 四、进模型的两条通路

**名单进 system 段,细节走工具。** 这是这个功能最要紧的一处判断。

理由是不对称的。两件事各自都不能靠模型自己想起来去查:

1. **长期在吃的是别的所有回答的前提。** 用户问「我最近静息心率怎么低了」,模型必须在**决定调
   哪个工具之前**就知道他在吃 β 阻滞剂。
2. **不能吃 / 试过没用是每次建议的前提。** 而模型不会在给建议之前先想「我要不要查一下他的
   禁忌」——它想不起来的那次,恰好就是它推荐了一个他过敏的东西的那次。

对比召回:召回漏了,用户补一句「上次不是说」就回来了,所以那边宁可漏。这边漏一次的代价
不是多等两个往返。所以用药**不做触发式挂载**,常驻。

### 4.1 system 段(`MedicationSnapshot.instructionBlock`)

```
关于他和药/补剂（他自己记的，不是健康数据）：
- 【不能吃】青霉素 — 过敏，起疹子
- 【长期】阿托伐他汀 — 医生开的，降血脂
- 【长期】维生素 D3 — 冬天日照少
- 【需要时】布洛芬 — 头疼时，半小时管用
- 【试过没用】褪黑素 — 睡不着时试过两周，没感觉

要提到吃什么之前先看这份表：他明确不能吃的绝对不要提；他试过没用的不要再推荐一次，
要提也得先说一句「你之前试过」。剂量一律不给建议，让他问医生或药师。
最近吃得怎么样、什么时候开始吃的，以 list_medications 返回的为准。
```

- 分组按第三节的顺序,**不能吃永远第一**。
- 每条一行:名字 + 一句(原因 / 触发条件 / 效果)。
- **上限 24 条 / 800 字**,超了先裁「试过了」那一组,**不能吃那组永远不裁**。裁掉的在工具里
  查得到。
- **会话开始时取好,会话之内不换**(同 `MemorySnapshot`)——system 段中途变化打掉 prompt
  缓存,也让模型对用户的认知在一条对话里跳变。引擎不许自己去读盘。
- 空表返回 nil,不要塞一句「（暂无）」——那只会让模型去解释为什么没有。
- **末尾那三句是这个功能真正的产出**,不是免责声明。前两句(不能吃 / 试过没用)决定了这张表
  有没有用,第三句是安全线。有测试盯着。

### 4.2 工具

| 工具 | 干什么 |
| --- | --- |
| `list_medications` | 全量:含被裁掉的、剂量备注、什么时候开始的、一般功效说明、他自己的效果评价 |
| `log_medication` | 对话里当场落一条(名字 / 状态 / 什么情况下吃 / 为什么),可带 `followUpDays` |
| `update_medication` | 更新效果和状态:他说「那个没用」「已经停了」时调 |

**写入是主路径,不是辅路径。** 这和记忆里 `remember` 的地位不一样——记忆的多数条目靠会话
结束后抽取补,而这张表的每一条都产生自一次明确的问答("我试试这个吧"),用户当场就该看见它
落下了。所以气泡上要出现「记下了:褪黑素(需要时)」,同 `remember`。

`update_medication` 是闭环的另一半。没有它,`outcome` 那一列永远是空的,而那正是这张表
最值钱的一列。

三个工具都受总开关和隐私会话管(见第八节)。

### 4.3 回访:这个功能的闭环

`followUpAt` 到期 → 进早上那条 check-in:「上周说要试的褪黑素,有感觉吗?」

**没有回访,这张表就只增不减**,三个月后是一份三十条的死清单,「试过了」那一组永远填不上,
而那一组才是它区别于一个记事本的地方。

复用 `MemoryKind.followUp` 已经铺好的那条路:`CheckInScheduler.content` 里排在触发点前面
(那是说好的事,触发点只是数据里冒出来的现象),点开通知就消掉。默认 14 天,补剂类可以长一点
(见效本来就慢)。**到期不等于删除**,同 `MemoryItem`:到期那一刻正是它要派上用场的时候。

## 五、独立入口与界面

**入口:`ChatView` toolbar leading 加第二颗 `pills`**,和会话列表并排。

不放设置里(记忆藏在设置里是对的——用户一年改两次;这张表他会反复打开看「我上次试的那个
叫啥来着」)。也不两处都放:「两个入口做同一件事」已经踩过一次(新对话那颗)。

三个页面:

- **`MedicationListView`** — 按第三节四组排。每行:名字 · 一句(触发条件或效果)· 来源标
  (自己加的 / Vana 记的)。空态那句要说清这张表是干嘛的:
  **「你吃过、在吃、不能吃的东西。Vana 每次给建议之前都会先看这里。」**
  ——不说这一句,用户会以为它是个用药提醒器,然后失望地发现它不提醒。
- **`MedicationDetailView`** — 名字、状态、什么情况下吃、为什么吃/谁让吃的、
  **他自己的效果评价**、一般功效说明(标「一般说明,不是给你的建议」)、备注、
  「说好 X 天后回来看」。底部一颗「问问 Vana」。
- **`MedicationEditView`** — 全字段可编辑。状态是一个四选一。

**「不能吃」那一组在界面上要显眼**(红色标、排最上面),并且删除要二次确认。它和别的行不是
同一种东西。

## 六、一条的字段

```
MedicationItem
  id            UUID
  name          名字
  status        cannotTake / ongoing / asNeeded / tried
  when          什么情况下吃（"头疼时" "冬天" "每天早上"）—— asNeeded 主要用
  reason        为什么吃 / 谁让吃的
  outcome       他自己的效果评价 ——【这张表最值钱的一列】
  brief         一般功效说明（模型生成，标明来源，见第七节）
  note          备注
  origin        manual 设置页手写 / asked 对话里让 Vana 记 / health 从 Apple Health 来
  followUpAt    说好过一阵子回来看
  startedAt / createdAt / updatedAt
  healthConceptId  预留：将来接 Apple Health 时对上号（见第八节）
```

`origin` 三态和 `MemoryItem` 完全一致,理由也一致:界面上每条都要能回答「这句哪来的」,
用户分不清是自己写的还是模型记的,就没法判断该不该信它。

## 七、一般功效怎么来(`MedicationBriefer`)

功效有两层,别混:

- **他自己的效果**(`outcome`)——他记的,或对话里说的。这一层值钱得多,永远优先展示。
- **一般功效**(`brief`)——这类东西通常是干什么的。模型生成。

生成走一次不带工具的便宜调用,和 `QuestionSuggester` / `FollowUpSuggester` 同一套做法。
加一条时跑一次,**结果存在这条上**,不每次渲染重算。

- **生成失败就空着**,不写「暂无」。列表照常用(同首屏建议的兜底逻辑)。
- **必须标明是生成的**。同 `MemoryItem.origin` 那条。
- **不写剂量,不写该不该吃**。只写这类东西一般是干什么的、常见注意点。措辞收在「一般说明」。
- 留一颗「重新生成」;用户改过的算手写,重新生成要二次确认(同 `MemoryStore` 的 pinned)。

## 八、Apple Health:旁证,不是另一半。第一版不接

它唯一能补上的是自有行回答不了的一件事:**「他说他在吃,但真的在按时吃吗」**。
解释数据异常时有用("你血压这几天高,日志显示上周有四天没打卡")。

但那是第二版的事,而且第一版接它是净亏:

- 多一个授权面板,而多数人还没用起来 Apple Health 的用药功能——首启多一次拒绝的机会。
- 要先验证 entitlement、是否 per-object 授权、补剂在里面长什么样,那是一整个前置任务。
- 镜像行和自有行的对称设计要多一套「哪些字段能在 Vana 改、哪些要跳去健康 App」的规则,
  而这张表的重心(状态、效果、禁忌)HealthKit 一个字段都没有。

**第一版:这张表全是 Vana 自己的。** 只保留 `healthConceptId` 和 `origin == .health` 两个
空位,将来接得上号。

真接的时候,边界照上一版的结论:**Apple Health 有的不存第二份**(名字、剂型、吃没吃),
存了就会漂——他在健康 App 停了药,Vana 还在跟模型说他在吃。

**故意不做的两件:**

- **不做提醒和打卡。** Apple Health 已经做了,做第二套只会两边对不上,而在这个功能里漏一次
  提醒的代价是真的。空态那句话必须说清这一点,否则用户会按提醒器来期待它。
- **不做交互作用数据库。** 查一张本地相互作用表,意味着 Vana 在给一个它兜不住的结论背书。
  模型按一般知识说注意点、把结论指向药师,是这里唯一诚实的形态。

## 九、以药为话题

`SessionThread.medication(UUID)`,和目标线**完全同构**:详情页那颗「问问 Vana」进来接着上次
那条聊,`SessionThreadPolicy` 用目标线那档(21 天 idle)——「我在试褪黑素」是一件长期的事,
出差一周回来还是同一件。列表页里按线合并成一行,同 `GoalSummary`。

system 段加一段 focus:

> 这条对话围绕他记下的「褪黑素」。状态:需要时吃;他记的原因是「睡不着」;
> 他自己的评价是「试了两周没感觉」。照常查健康数据回答,把变化和这件事挂上钩,
> 但不要建议他调整剂量或停药。

**不给话题**(同延续线那条):今天问副作用、明天问要不要继续,话题写死在 system 段里第三天
就和正在问的事对不上了。

**那颗按钮不预填问题。** 空会话那三条建议由本地拼(同 `ChatTopic.questions`,不花钱),
按状态给不同的三条——「试过了」那一类问的是「要不要换一个」,「长期在吃」问的是
「和我最近的数据有关系吗」。

## 十、安全边界

写进 system 段,一条都不能少:

- **不建议增减剂量、不建议停药、不建议换药**,一律「这要问开药的医生或药师」。
- **`status == .cannotTake` 是硬约束**,不是一条参考信息。措辞用「绝对不要提」。
- **交互作用**:可以说「这两类一起吃常见的注意点是…,具体要问药师」,不下「你可以/不可以」
  的结论。
- 数据异常和某种药可能相关时,说明可能相关 + 这不是诊断,**不要反推「所以你该停药」**。

## 十一、和记忆的隔离

**`MemoryExtractor` 不许抽用药。** 它只看用户和助手说过的话,而「我在吃他汀」「我对青霉素
过敏」正是它最爱抽的那种 profile 记忆——抽了就是同一件事的两份,一份在设置里改、另一份在
用药页改,迟早对不上,而对不上的那次可能是禁忌那一条。

抽取的 prompt 里明写一句「用药和补剂不要记,那有专门的地方」。**这条最容易漏,要有测试。**

## 十二、隐私

- `medications.json` 用 `.completeFileProtection`。这张表比「他喜欢早睡」敏感一档。
  (顺带考虑把 `memory.json` 一起升上去。)
- **隐私会话照样读,不挂那三个写工具**——承诺的是不往盘上写,不是失忆。同记忆。
  禁忌那一组在隐私会话里尤其不能关掉:承诺的是不留痕迹,不是不管他死活。
- 设置里一个总开关:关掉后 system 段不带名单、三个工具都不挂。**列表页入口留着**——数据是
  他的,不因为关掉一个开关就看不见。
- 这个开关**不归在 `memoryEnabled` 下面**。关掉记忆的人不指望 Vana 还记得他说过的话,
  但他仍然会指望这张自己维护的表还在。

## 十三、文件清单

新增 `Vana/Medications/`:

| 文件 | 内容 |
| --- | --- |
| `MedicationItem.swift` | `MedicationItem` + `MedicationStatus` + `MedicationSnapshot` |
| `MedicationStore.swift` | actor,`Documents/medications.json`,`init(directory:)` 必须有 |
| `MedicationTools.swift` | 三个工具的 `CapabilityRegistry` |
| `MedicationBriefer.swift` | 一般功效生成 |
| `MedicationListView.swift` / `MedicationDetailView.swift` / `MedicationEditView.swift` | 界面 |

改动:

- `CapabilityRegistry.healthChat(...)` 加 `allowsMedicationWrites` + `medicationStore`
- `AIKitEngine` 加 `medications: MedicationSnapshot`,以及 `.medication` 线的 focus 文案
- `ChatViewModel` 会话开始时取快照(和 `memory` 并排)
- `SessionThread` 加 `.medication(UUID)`;`SessionThreadPolicy` 走目标线那档 idle
- `CheckInScheduler.content` 把到期的用药回访排进去(紧挨 `MemoryKind.followUp`)
- `MemoryExtractor` prompt 加一句「用药不归你管」
- `ChatView` toolbar 加一颗
- `EngineSettings` 加总开关;`SettingsView` 加那一行
- `xcodegen` 重新生成

## 十四、测试(`VanaTests/MedicationTests.swift`)

- 快照真的进了 system 段,分组顺序对,**不能吃排第一**
- **末尾那三句在**(不能吃 / 试过没用 / 剂量问医生)——这是这个功能的产出,必须有测试
- 超上限时先裁「试过了」,**`cannotTake` 一条都不裁**
- 隐私会话不挂三个写工具;总开关关掉时都不挂、system 段不带名单
- `update_medication` 能把 `ongoing` 改成 `tried` 并写上 `outcome`
- 到期的回访进了 `CheckInScheduler.content`
- 对话里说「我在吃维生素 D」,会话结束后 `MemoryView` 里**没有**这一条,用药页里有
- `MedicationStore(directory:)` 走临时目录——app 侧测试跑在 app host 里,
  `MedicationStore.shared` 就是模拟器上那份真的文件(`MemoryStore` 那条血泪)

## 十五、落地状态

T-M1 到 T-M7 **已实现**(2026-08-09),架构决策已并进 `CLAUDE.md`。

| | 内容 | 状态 |
| --- | --- | --- |
| T-M1 | `MedicationStore` + 模型 + 三个页面 | ✅ |
| T-M2 | system 段名单 + `list_medications` + 安全文案 | ✅ |
| T-M3 | `log_medication` + `update_medication` | ✅ |
| T-M4 | 回访闭环(到期进 check-in,点开只清约定不删记录) | ✅ |
| T-M5 | `MedicationBriefer` 一般说明 | ✅ |
| T-M6 | `SessionThread.medication` 以药为话题 | ✅ |
| T-M7 | 抽取器隔离 + 总开关 + `.completeFileProtection` | ✅ |
| T-M8 | (第二版)Apple Health 旁证 | 未做 |

模拟器上验过的一条端到端:清单里有一条「Melatonin / 需要时吃 / 我自己的评价:两周没效果」,
问「什么情况下该吃它？」,回答第一句是——

> 关于褪黑素——先说一句：你之前试过，两周后自己的评价是「没效果」。所以我不把它当成对你有效的
> 选项来推荐，只说它一般用在什么情况，具体怎么调整问开药的医生或药师。

这正是第三节说的那个隐藏价值:`outcome` 那一列拦住了一次重复推荐,而剂量那条线也守住了。

### T-M8 真要做时,先验证这三件

**先验证再写**,这三条会决定形态:

1. 读 `HKUserAnnotatedMedication` / `HKObjectType.medicationDoseEventType()` 需不需要额外
   entitlement 和 usage description。
2. 这两个类型是不是 `requiresPerObjectAuthorization`(每种药单独授权,会影响授权面板的样子)。
3. 补剂和维生素在 `HKUserAnnotatedMedication` 里长什么样。

边界仍照第八节:**Apple Health 有的不存第二份**,它补的只是「他说在吃,但真的在按时吃吗」。
